package MyCod.StudySkillbox.Modul_26;

public class Calendars {
   /*Calendar.getInstance();
   Calendar.getInstance(TimeZone zone);
   Calendar.getInstance(Locale locale);
   Calendar.getInstance(TimeZone zone, Locale locale);*/
/*Border border = BorderFactory.createBevelBorder(int type);
CompoundBorder border = BorderFactory.createCompoundBorder(Border outsideBorder, Border insideBorder);
Border border = BorderFactory.createLineBorder(Color color);
MatteBorder border = BorderFactory.createMatteBorder(int top, int left, int bottom, int right, Icon tileIcon);
TitledBorder border = BorderFactory.createTitledBorder(Border border);*/
}
